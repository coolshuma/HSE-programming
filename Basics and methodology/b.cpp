#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
#include <map>

using namespace std;

int main() {
    string word;
    map<string, int> def;

    while (cin >> word) {
        ++def[string]
    }
    vector 
    for (map<string, int>:: iterator it = def.begin(); it != def.end()) {
        
    }
    return 0;
}
